
export const Button = ({ onPress, name }) => {
    return (
        <button onClick={onPress} style={{
           
            
        }}>
            {name}
        </button>
    );
}

export default Button;